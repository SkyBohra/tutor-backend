# FastAPI & Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
motor==3.3.2
pymongo==4.6.1
beanie==1.25.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# AI & LLM (using openai directly - langchain removed for size)
openai==1.12.0
# langchain==0.1.6  # Removed - too large for Vercel serverless
# langchain-openai==0.0.5
# langchain-community==0.0.19

# Image/Video Generation
pillow==10.2.0
# replicate==0.23.1  # Removed - not essential, using httpx for API calls
# stability-sdk==0.8.4  # Removed - requires old grpcio incompatible with Python 3.12

# Avatar & Text-to-Speech (using httpx for ElevenLabs API directly)
# elevenlabs==0.2.27  # Removed - using httpx for direct API calls
gtts==2.5.0

# Visualization - REMOVED for Vercel size limit
# matplotlib==3.8.2  # Removed - too large for serverless
# numpy==1.26.3  # Removed - too large for serverless

# Caching & Queue - REMOVED (not needed for serverless)
# redis==5.0.1
# celery==5.3.6

# Utilities
python-dotenv==1.0.1
pydantic==2.6.0
pydantic-settings==2.1.0
aiofiles==23.2.1
httpx==0.26.0
websockets==12.0

# CORS
starlette==0.35.1

# Logging
loguru==0.7.2

# Testing (removed for production - install locally with: pip install pytest pytest-asyncio)
# pytest==8.0.0
# pytest-asyncio==0.23.4
